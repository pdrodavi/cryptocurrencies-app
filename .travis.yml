language: java
sudo: true
dist: trusty
jdk:
  - oraclejdk15
addons:
  apt:
    packages:
      - oracle-java15-installer
before_install:
  - chmod +x mvnw
deploy:
  provider: heroku
  api_key:
    secure: $HEROKU_API_KEY
  app: cryptovaluebtc
  on:
    repo: pdrodavi/cryptocurrencies-app
